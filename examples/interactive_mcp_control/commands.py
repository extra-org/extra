from __future__ import annotations

COMMANDS = """Commands:
  /help             Show this help
  /tools            List dynamically discovered MCP tools
  /history          Show current in-memory conversation history
  /approvals        Show current session approvals
  /session          Show session and runtime information
  /new-session      Start a new empty logical session
  /clear            Clear chat history only
  /clear-approvals  Clear approvals for the current session
  /exit, /quit      Close the engine and exit
"""
