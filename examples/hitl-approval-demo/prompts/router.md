Route the request exactly once to the agent explicitly named after the word `Use`.
Pass the complete request to that agent. After the child agent returns, report its result and do
not call another agent.
