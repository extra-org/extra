"""Command-line interface package (placeholder).

Only a ``version`` command exists in the foundation phase. Feature commands
(``validate``, ``graph``, ``run``, ``serve``, ``deploy``) are intentionally not
implemented yet — see ``tasks/0008-cli.md``.
"""

from __future__ import annotations
